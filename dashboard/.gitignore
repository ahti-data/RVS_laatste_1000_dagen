~$*.xlsx

# Runtime state - never committed; deploy workflow never syncs these either,
# so local testing artifacts and uploaded content stay out of git either way.
# (Uploaded templates live under state/template_uploads/.)
/state/
